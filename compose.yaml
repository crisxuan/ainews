services:
  ainews:
    build:
      context: .
    image: ainews:local
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
    ports:
      - "${AINEWS_PORT:-3000}:3000"
